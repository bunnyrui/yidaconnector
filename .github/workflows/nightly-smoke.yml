name: Nightly Smoke

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    name: Real Yida Smoke
    runs-on: ubuntu-latest
    env:
      YIDACONNECTOR_SMOKE_COOKIES_BASE64: ${{ secrets.YIDACONNECTOR_SMOKE_COOKIES_BASE64 }}
      YIDACONNECTOR_SMOKE_APP_TYPE: ${{ secrets.YIDACONNECTOR_SMOKE_APP_TYPE }}
      YIDACONNECTOR_SMOKE_FORM_UUID: ${{ secrets.YIDACONNECTOR_SMOKE_FORM_UUID }}
      YIDACONNECTOR_SMOKE_BASE_URL: ${{ secrets.YIDACONNECTOR_SMOKE_BASE_URL }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci --ignore-scripts

    - name: Run real-environment smoke
      run: node scripts/nightly-smoke.js
